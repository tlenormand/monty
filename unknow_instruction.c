#include "monty.h"

/**
 * unknow_instruction - error for unknow instruction
 * @stack: pointer to the address of the stack
 * Return: -99 for unknow instruction error code
 */

int unknow_instruction(stack_t **stack)
{
	(void) **stack;

	return (-99);
}
